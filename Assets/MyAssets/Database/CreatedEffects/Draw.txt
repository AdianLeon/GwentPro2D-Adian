effect{
	Name: "Draw",
	Action: (targets, context) => {
		topCard = context.Deck.Pop();
		player=context.TriggerPlayer;
		hand=context.HandOfPlayer(player);
		hand.SendBottom(topCard);
		hand.Shuffle(); 
	}
}